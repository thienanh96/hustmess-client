import { SearchRoomchatPipe } from './search-roomchat.pipe';

describe('SearchRoomchatPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRoomchatPipe();
    expect(pipe).toBeTruthy();
  });
});
